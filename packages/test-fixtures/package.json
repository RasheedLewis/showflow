{
  "name": "@showflow/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic Showflow builders and sample test data.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@showflow/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vitest": "4.1.10"
  }
}
