{
  "name": "@showflow/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure Showflow domain entities, invariants, and transformations.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  }
}
