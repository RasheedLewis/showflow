{
  "name": "@showflow/resources",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Showflow Resource metadata, validation, and file abstractions.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@showflow/domain": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  }
}
